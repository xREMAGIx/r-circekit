---
export interface Props {
  raw: string;
}

const { raw } = Astro.props;

const html = await Astro.slots.render("default");
import { Code } from "astro/components";
---

{html && <Code code={html} lang="html" />}
{raw && <Code code={raw} lang="html" />}
